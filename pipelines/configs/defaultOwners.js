// default veto code reviewers who can review any code just in case service specific owners are not available, specify only name and their mapping is in ./ownerUsername.js
const defaultOwners = [];

module.exports = defaultOwners;
